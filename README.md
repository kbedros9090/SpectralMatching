1. Uses the Pandas library to read the csv files. 
2. Then, numpy is used to extract the data (1d array) because a list is no bueno
4. Create a common grid, finding the biggest step so no fake details
5. Interpolate the data by estimating the values between the data. 
6. Calculate the SAM which is the dot product
7. Find the cosine similarity and normalization (linear). 
8. Plot the graph using matplotlib. 
